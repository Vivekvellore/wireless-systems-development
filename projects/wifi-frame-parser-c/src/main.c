#include <stdio.h>
#include "cutils/log.h"
int main(int argc, char** argv) {
  LOGI("wifi-frame-parser starting (stub)");
  // TODO: open raw socket/pcap, decode beacons & IEs (incl. HE/6E)
  return 0;
}
